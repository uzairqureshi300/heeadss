apply plugin: 'com.android.library'

version = 26

android {
    compileSdkVersion 26

    defaultConfig {
        minSdkVersion 15
    }

    lintOptions {
        abortOnError false
    }
}

dependencies {
    compile 'com.pnikosis:materialish-progress:1.0'
}

